export function validateEmail(email) {
	/*
    TODO Fetch email from MongoDB and compare it with the "email" parameter
    */
	return email === "test@email.com";
}

export function validatePassword(password) {
	/*
    TODO Fetch password from MongoDB and compare it with the "password" parameter
    */
	return password === "pwd";
}
