import React from "react";

const gridStyle = {
	display: "flex",
	justifyContent: "space-between",
	padding: "1rem",
	margin: "1rem",
	borderBottom: "0.1rem #c0c0c0 solid",
};

function DynamicGrid(props) {
	return <div className='' style={gridStyle}></div>;
}

export default DynamicGrid;
